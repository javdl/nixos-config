# Placeholder for github-runner-02 secrets
# This file will be encrypted with SOPS after provisioning once the server's age key is known.
#
# To set up:
#   1. Provision the server: make hetzner/provision NIXADDR=<ip> NIXNAME=github-runner-02
#   2. Get age key: ssh root@<ip> 'cat /etc/ssh/ssh_host_ed25519_key.pub' | ssh-to-age
#   3. Update .sops.yaml with real age key (replace placeholder)
#   4. Encrypt: sops secrets/github-runner-02.yaml
#
# Required keys:
#   github-runner-token: ghp_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
