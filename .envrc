export PATH="$HOME/.local/bin:$PATH"

# Load the flake devShell (nixfmt, statix, deadnix, sops, ssh-to-age, age).
# Requires nix-direnv: https://github.com/nix-community/nix-direnv
if has use_flake; then
  use flake
fi
